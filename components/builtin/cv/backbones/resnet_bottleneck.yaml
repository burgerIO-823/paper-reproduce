name: "resnet_bottleneck"
type: "cv.backbone"
domain: "cv"
spec_version: "1.0"
description: "ResNet bottleneck residual block (1x1→3x3→1x1 convolutions with skip connection)"

source:
  paper: "Deep Residual Learning for Image Recognition"
  arxiv_id: "1512.03385"
  url: "https://arxiv.org/abs/1512.03385"

interface:
  inputs:
    x: "(B, in_channels, H, W)"
  outputs:
    output: "(B, out_channels, H//stride, W//stride)"

dependencies:
  - package: "torch>=1.8"

tags:
  - resnet
  - bottleneck
  - residual
  - classification
  - backbone

code_file: "resnet_bottleneck.py"
